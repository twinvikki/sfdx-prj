public class ClassTriggerHandler {

    public static void ClassStudent(list<class__c> newlist, map<id,class__c> oldmap){
        set<string> ClassName = new set<string>();
        set<id> Classid = new set<id>();
        integer countofchildrecords;
        for(class__c c:newlist){
            if(classid.contains(c.id)){
                
            }
        }

        }  
}